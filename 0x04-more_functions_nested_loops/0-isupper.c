#include "main.h"

/**
 * _isupper - checks for uppercase character
 * @c: takes in argument to check
 * Return: Always (Success)
 */

int _isupper(int c)
{
	if (x >= 65 && x <=90)
	{
		return (1);
	}
	return (0);
}
